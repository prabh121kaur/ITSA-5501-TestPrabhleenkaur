name: DevOps Test

on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run echo message
        run: echo "push detected on main branch! Workflow executed successfully"
